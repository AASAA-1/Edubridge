<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <!-- Divider between entries (hidden for first item) -->
    <View
        android:id="@+id/divider_analysis"
        android:layout_width="match_parent"
        android:layout_height="@dimen/divider_height"
        android:background="?attr/colorBorderCustom"
        android:layout_marginTop="@dimen/margin_standard"
        android:layout_marginBottom="@dimen/margin_standard" />

    <!-- Clickable header: date + chevron -->
    <LinearLayout
        android:id="@+id/header_analysis"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="?attr/selectableItemBackground"
        android:clickable="true"
        android:focusable="true"
        android:paddingTop="@dimen/margin_tiny"
        android:paddingBottom="@dimen/margin_tiny">

        <TextView
            android:id="@+id/tv_analysis_date"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textSize="@dimen/text_xs"
            android:textColor="?attr/colorSecondaryCustom"
            android:textStyle="bold" />

        <ImageView
            android:id="@+id/iv_chevron"
            android:layout_width="@dimen/analysis_chevron_size"
            android:layout_height="@dimen/analysis_chevron_size"
            android:src="@drawable/ic_chevron_down"
            android:contentDescription="@string/analysis_result_toggle_cd" />

    </LinearLayout>

    <!-- Analysis text (shown when expanded) -->
    <TextView
        android:id="@+id/tv_analysis_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="@dimen/text_small"
        android:textColor="?attr/colorTextCustom"
        android:lineSpacingExtra="@dimen/line_spacing_extra"
        android:paddingTop="@dimen/margin_small"
        android:visibility="gone" />

</LinearLayout>
